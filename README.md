## Public journal/story site with single-author posting, media attachments, and II-gated locked posts

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
